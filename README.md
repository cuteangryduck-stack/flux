# KLL's Flux Video Calling Fixes
This repo is for fixing up Kinetic Logic Lab's Flux Video Call, since it has many bugs from being literally vibe coded.

*Note from Kinetic Logic Labs*
To properly deploy, you need a custom YAML/workflow run by creating a file in /github/workflows. Also, wibe coding takes a bunch of time to get the UI and UX good, so don't judge from that. It also looks better. If you want an actual working version, just go to kineticlogiclabs.github.io/flux. 

*Note from Akon'Vess*
I just removed all the extra yml's. Apparently it was trying to use a gemini api as well as trying to fetch info at /flux, while i was using a custom domain, which meant it had the wrong file path. also you spelled "wibe coding" wrong, it's 'vibe coding" also yes i will judge

it works now :D
